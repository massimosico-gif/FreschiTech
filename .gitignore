# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies
node_modules
**/node_modules

# Build outputs
dist
dist-ssr
**/target
**/dist

# OS files
.DS_Store

# Tauri specific
src-tauri/target
src-tauri/binaries
src-tauri/gen

# Env files
*.local
.env
.env.*

# Editor files
.vscode
.idea
*.swp
*.swo

# Temporary and Build artifacts
x max - DB CONTABILITÀ.xlsx
PDF esempio (eliminare a test finiti)
*.tar.gz
*.sig

# ---------------------------------------------------------------------------
# SEGRETI — non devono MAI finire nel repository
# (letti da build.rs e incorporati nel binario, vedi CLAUDE.md)
# ---------------------------------------------------------------------------
GitHubToken.env
# Qualunque variante, non solo il nome esatto: generando la chiave nuova come
# `signing_key_nuova.txt` si sarebbe ottenuto un file NON ignorato contenente
# una chiave privata, in un repository pubblico. Cioe' di nuovo il difetto che
# la rotazione sta correggendo.
signing_key*.txt
signing_key*.txt.pub
modern-ui/src-tauri/secrets/
db_key
diagnostics.json

# ---------------------------------------------------------------------------
# DATI REALI DEI CLIENTI
# Il database contiene ragioni sociali, partite IVA, indirizzi e importi.
# ---------------------------------------------------------------------------
*.db
*.sqlite
*.sqlite3

# ---------------------------------------------------------------------------
# PACCHETTI DI RELEASE (vanno pubblicati come asset, non versionati)
# ---------------------------------------------------------------------------
*.tar.gz
*.tar.gz.sig
*.dmg

# Artefatti Python degli script di release
__pycache__/
*.pyc
